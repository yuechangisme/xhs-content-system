/**
 * xhs-content-system 配置示例
 *
 * 复制此文件为 config.local.js 并填充本机路径。
 * config.local.js 已在 .gitignore 中，不会上传。
 *
 * const config = {
 *   // Chrome 可执行文件路径
 *   // 留空 = 使用 puppeteer 自动查找
 *   chromePath: '',
 *
 *   // 小红书 Cookie 文件（JSON 格式）
 *   // 从 creator.xiaohongshu.com 导出 cookie 后保存为此文件
 *   cookiePath: '',
 * };
 *
 * module.exports = config;
 */
